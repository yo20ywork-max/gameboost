[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gameboost-client"
version = "2.0.0"
description = "GameBoost Windows desktop client"
requires-python = ">=3.10"
dependencies = [
  "PySide6==6.9.0",
  "requests==2.32.3"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
